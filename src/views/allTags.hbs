<div class="container">
    <article class="tag-card">
        {{#each tag}}
            <h2 class="name">{{name}}</h2>
            {{else}}
                <p class="empty">No content</p>
        {{/each}}
    </article>
    <a href="/">Go Back</a>
</div>