<div class="container">
    <article class="author-card">
        {{#each author}}
            <h2 name="name">{{name}}</h2>
            {{else}}
                <p class="empty">No content</p>
        {{/each}}
    </article>
</div>